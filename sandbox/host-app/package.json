{
  "name": "host-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd .. && fe build shell",
    "check": "cd .. && fe check shell",
    "typecheck": "bunx tsc --noEmit",
    "test": "playwright test"
  },
  "dependencies": {
    "@fe/runtime": "workspace:*"
  },
  "devDependencies": {
    "@acme/fe.mfe-b": "file:../mfe-b",
    "@fe/cli": "workspace:*"
  }
}
