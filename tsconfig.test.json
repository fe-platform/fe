{
	"$schema": "https://json.schemastore.org/tsconfig",
	"extends": "./tsconfig.base.json",
	"compilerOptions": {
		"types": ["@types/bun"],
		"noEmit": true,
	},
	"exclude": ["node_modules", "dist", "build"],
}
