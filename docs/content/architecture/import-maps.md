---
sidebar_position: 4
---

# Multiple Import Maps

How the platform uses multiple browser import maps (Chrome 133+, Safari 18.4+) for lazy dependency resolution, deduplication, and per-consumer scoping.

<!-- TODO: Explain the multiple import map mechanism, how maps are injected lazily before dynamic imports, how deduplication works (first entry wins), and how scopes handle version conflicts. Include browser support table. -->
