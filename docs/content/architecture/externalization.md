---
sidebar_position: 5
---

# Externalization

How `fe()` imports stay external at build time. The naming convention itself is the build signal — no extra config needed.

<!-- TODO: Explain how readFeDepKeys filters devDependencies starting with "fe(", passes them to Bun.build's external option, and why this convention-over-configuration approach works. Show package.json examples. -->
