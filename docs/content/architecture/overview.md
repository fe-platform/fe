---
sidebar_position: 1
---

# Architecture Overview

High-level mental model of the fe platform: how MFEs are built, published, resolved, and composed at runtime using nothing but browser-native primitives.

<!-- TODO: Cover the full lifecycle diagram — author → build → publish → resolve → inject → import → render. Explain how each package fits into this pipeline. -->
