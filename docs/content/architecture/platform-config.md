---
sidebar_position: 8
---

# Platform Config

The `platform.json` schema: routes, packages, versions, and dependency declarations.

<!-- TODO: Document the full platform.json schema with annotated examples. Cover the routes section (URL path → specifier@version), packages section (specifier → versions → {url, deps}), and how fe publish writes to this file. -->
