---
sidebar_position: 2
---

# The `fe()` Specifier

How `fe(@scope/name)` works as a plain package name — not a URL scheme. It is the `name` in `package.json`, a bare specifier in `import` statements, and the key in the platform's lookup service.

<!-- TODO: Explain naming convention, why it was chosen, how it maps to npm package names, how build tools recognize it, and how the browser resolves it at runtime via import maps. Include code examples. -->
