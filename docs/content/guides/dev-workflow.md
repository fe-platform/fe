---
sidebar_position: 1
---

# Dev Workflow

Isolated MFE development with `fe dev`, hot module replacement via SSE, and the rebuild cycle.

<!-- TODO: Explain fe dev in depth: how the sandbox works, file watching, Bun rebuild, SSE notification, module swap without page reload. Include the HMR internals (pendingTs, cache-busting URLs). -->
