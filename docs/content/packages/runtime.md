---
sidebar_position: 2
---

# @fe/runtime

Browser runtime for the fe platform — config reading, semver resolution, import map injection, and the `load()` function that orchestrates MFE mounting.

<!-- TODO: Document the public API (load, loadDevtools), the resolution algorithm, import map injection strategy, and how multiple maps are deduped across navigations. -->
